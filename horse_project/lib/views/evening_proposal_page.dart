import 'package:flutter/material.dart';

class EveningProposalPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Evening Proposal'),
      ),
      body: Center(
        child: Text('Evening Proposal Page'),
      ),
    );
  }
}
