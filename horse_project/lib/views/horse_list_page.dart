import 'package:flutter/material.dart';

class HorseListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Horse List'),
      ),
      body: Center(
        child: Text('Horse List Page'),
      ),
    );
  }
}
